DNA双螺旋是Three.js的理想场景——螺旋结构通过3D旋转才能真正展现其空间美感。我构建了一个包含约30个碱基对的DNA模型，磷酸骨架沿双螺旋轨迹排列，碱基对（A-T、G-C）用不同颜色区分，并标注了碱基名称。

```show-widget
{"title": "dna_double_helix", "widget_code": "<style>\ncanvas { display: block; width: 100% !important; height: 420px !important; }\n#controls { display: flex; align-items: center; gap: 12px; padding: 10px 0 0; font-size: 13px; color: var(--color-text-secondary); }\n</style>\n<canvas id=\"c\"></canvas>\n<div id=\"controls\">\n  <button onclick=\"toggleRotation()\">暂停旋转</button>\n  <span style=\"flex:1\">拖动旋转查看 · 滚轮缩放</span>\n  <span style=\"display:flex;align-items:center;gap:6px\"><span style=\"width:10px;height:10px;border-radius:2px;background:#378ADD\"></span>A</span>\n  <span style=\"display:flex;align-items:center;gap:6px\"><span style=\"width:10px;height:10px;border-radius:2px;background:#EF9F27\"></span>T</span>\n  <span style=\"display:flex;align-items:center;gap:6px\"><span style=\"width:10px;height:10px;border-radius:2px;background:#639922\"></span>G</span>\n  <span style=\"display:flex;align-items:center;gap:6px\"><span style=\"width:10px;height:10px;border-radius:2px;background:#D85A30\"></span>C</span>\n</div>\n<script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js\" onload=\"init()\"></script>\n<script>\nvar scene, camera, renderer, controls, dnaGroup;\nvar rotating = true;\n\nfunction init() {\n  if (!window.THREE || !THREE.OrbitControls) return;\n  var canvas = document.getElementById('c');\n  var W = canvas.clientWidth, H = 420;\n  renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true, alpha: true });\n  renderer.setSize(W, H);\n  renderer.setPixelRatio(Math.min(devicePixelRatio, 2));\n\n  scene = new THREE.Scene();\n  camera = new THREE.PerspectiveCamera(45, W / H, 0.1, 1000);\n  camera.position.set(0, 4, 14);\n\n  scene.add(new THREE.AmbientLight(0xffffff, 0.7));\n  var sun = new THREE.DirectionalLight(0xffffff, 0.9);\n  sun.position.set(5, 8, 5);\n  scene.add(sun);\n  var fill = new THREE.DirectionalLight(0xffffff, 0.4);\n  fill.position.set(-5, 0, -5);\n  scene.add(fill);\n\n  controls = new THREE.OrbitControls(camera, canvas);\n  controls.enableDamping = true;\n  controls.dampingFactor = 0.08;\n  controls.autoRotate = true;\n  controls.autoRotateSpeed = 0.8;\n\n  dnaGroup = new THREE.Group();\n  buildDNA();\n  scene.add(dnaGroup);\n\n  function animate() {\n    requestAnimationFrame(animate);\n    controls.update();\n    renderer.render(scene, camera);\n  }\n  animate();\n}\n\nfunction buildDNA() {\n  var basePairs = 32;\n  var radius = 1.8;\n  var rise = 0.65;\n  var twist = 0.35;\n  \n  var colors = {\n    A: 0x378ADD,\n    T: 0xEF9F27,\n    G: 0x639922,\n    C: 0xD85A30,\n    backbone: 0x888780\n  };\n  \n  var sequence = ['A','T','G','C','A','T','C','G','T','A','G','C','A','T','G','C','A','T','C','G','A','T','G','C','T','A','C','G','A','T','G','C'];\n  \n  var backboneMat = new THREE.MeshLambertMaterial({ color: colors.backbone });\n  var sphereGeo = new THREE.SphereGeometry(0.22, 16, 16);\n  \n  for (var i = 0; i < basePairs; i++) {\n    var y = (i - basePairs/2) * rise;\n    var angle = i * twist * Math.PI * 2;\n    var nextAngle = (i + 1) * twist * Math.PI * 2;\n    \n    var x1 = Math.cos(angle) * radius;\n    var z1 = Math.sin(angle) * radius;\n    var x2 = Math.cos(angle + Math.PI) * radius;\n    var z2 = Math.sin(angle + Math.PI) * radius;\n    \n    var bb1 = new THREE.Mesh(sphereGeo, backboneMat);\n    bb1.position.set(x1, y, z1);\n    dnaGroup.add(bb1);\n    \n    var bb2 = new THREE.Mesh(sphereGeo, backboneMat);\n    bb2.position.set(x2, y, z2);\n    dnaGroup.add(bb2);\n    \n    if (i < basePairs - 1) {\n      var yNext = (i + 1 - basePairs/2) * rise;\n      var x1n = Math.cos(nextAngle) * radius;\n      var z1n = Math.sin(nextAngle) * radius;\n      var x2n = Math.cos(nextAngle + Math.PI) * radius;\n      var z2n = Math.sin(nextAngle + Math.PI) * radius;\n      \n      var strand1 = createCylinder(new THREE.Vector3(x1, y, z1), new THREE.Vector3(x1n, yNext, z1n), 0.08, colors.backbone);\n      var strand2 = createCylinder(new THREE.Vector3(x2, y, z2), new THREE.Vector3(x2n, yNext, z2n), 0.08, colors.backbone);\n      dnaGroup.add(strand1);\n      dnaGroup.add(strand2);\n    }\n    \n    var base1 = sequence[i];\n    var base2 = base1 === 'A' ? 'T' : base1 === 'T' ? 'A' : base1 === 'G' ? 'C' : 'G';\n    \n    var baseGeo = new THREE.CylinderGeometry(0.15, 0.15, radius * 0.75, 12);\n    baseGeo.rotateZ(Math.PI / 2);\n    \n    var mat1 = new THREE.MeshLambertMaterial({ color: colors[base1] });\n    var mat2 = new THREE.MeshLambertMaterial({ color: colors[base2] });\n    \n    var baseMesh1 = new THREE.Mesh(baseGeo, mat1);\n    baseMesh1.position.set(x1 * 0.6, y, z1 * 0.6);\n    baseMesh1.lookAt(0, y, 0);\n    dnaGroup.add(baseMesh1);\n    \n    var baseMesh2 = new THREE.Mesh(baseGeo, mat2);\n    baseMesh2.position.set(x2 * 0.6, y, z2 * 0.6);\n    baseMesh2.lookAt(0, y, 0);\n    dnaGroup.add(baseMesh2);\n    \n    var connector = createCylinder(new THREE.Vector3(x1 * 0.25, y, z1 * 0.25), new THREE.Vector3(x2 * 0.25, y, z2 * 0.25), 0.06, 0xaaaaaa);\n    dnaGroup.add(connector);\n  }\n}\n\nfunction createCylinder(v1, v2, radius, color) {\n  var direction = new THREE.Vector3().subVectors(v2, v1);\n  var arrow = new THREE.ArrowHelper(direction.clone().normalize(), v1);\n  var rotation = new THREE.Vector3().setFromEuler(arrow.rotation);\n  arrow.rotation.set(0, 0, 0);\n  \n  var length = direction.length();\n  var geometry = new THREE.CylinderGeometry(radius, radius, length, 8, 1);\n  geometry.rotateX(Math.PI / 2);\n  var material = new THREE.MeshLambertMaterial({ color: color });\n  var cylinder = new THREE.Mesh(geometry, material);\n  cylinder.position.copy(v1).add(direction.multiplyScalar(0.5));\n  cylinder.lookAt(v2);\n  return cylinder;\n}\n\nfunction toggleRotation() {\n  rotating = !rotating;\n  controls.autoRotate = rotating;\n  document.querySelector('#controls button').textContent = rotating ? '暂停旋转' : '继续旋转';\n}\n\nif (window.THREE && THREE.OrbitControls) init();\n</script>"}
```

**结构说明**：灰色球体和连杆构成糖-磷酸骨架，沿着双螺旋轨迹盘旋上升；中间彩色横条代表碱基对——蓝色（A）与琥珀色（T）配对，绿色（G）与珊瑚色（C）配对，遵循查格夫法则。每条螺旋链上的碱基序列相互互补，通过氢键（灰色细杆）连接形成稳定的双螺旋构象。