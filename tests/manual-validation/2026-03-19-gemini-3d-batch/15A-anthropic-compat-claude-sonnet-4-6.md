DNA 双螺旋由两条互补链围绕同一轴线盘旋而成，碱基对在内部通过氢键连接，磷酸糖骨架在外侧支撑整体结构。

```show-widget
{"title": "dna_double_helix_3d", "widget_code": "<style>\ncanvas { display: block; width: 100% !important; height: 460px !important; }\n#controls { display: flex; align-items: center; gap: 12px; padding: 10px 0 0; font-size: 13px; color: var(--color-text-secondary); flex-wrap: wrap; }\n</style>\n<canvas id=\"c\"></canvas>\n<div id=\"controls\">\n  <button onclick=\"toggleSpin()\">暂停旋转</button>\n  <label style=\"display:flex;align-items:center;gap:8px;flex:1;min-width:160px\">\n    旋转速度 <input type=\"range\" min=\"1\" max=\"10\" value=\"4\" step=\"1\" style=\"flex:1\" oninput=\"spinSpeed=this.value*0.003\">\n  </label>\n  <span style=\"font-size:12px;opacity:0.7\">拖拽可旋转视角 · 点击碱基了解详情</span>\n</div>\n<script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js\" onload=\"init()\"></script>\n<script>\nvar scene, camera, renderer, controls;\nvar spinning = true;\nvar spinSpeed = 0.012;\nvar helixGroup;\n\nfunction init() {\n  if (!window.THREE || !THREE.OrbitControls) return;\n  var canvas = document.getElementById('c');\n  var W = canvas.clientWidth, H = 460;\n  renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true, alpha: true });\n  renderer.setSize(W, H);\n  renderer.setPixelRatio(Math.min(devicePixelRatio, 2));\n\n  scene = new THREE.Scene();\n  camera = new THREE.PerspectiveCamera(45, W / H, 0.1, 1000);\n  camera.position.set(0, 0, 22);\n\n  scene.add(new THREE.AmbientLight(0xffffff, 0.7));\n  var sun = new THREE.DirectionalLight(0xffffff, 0.9);\n  sun.position.set(5, 10, 8);\n  scene.add(sun);\n  var fill = new THREE.DirectionalLight(0xffffff, 0.3);\n  fill.position.set(-5, -5, -5);\n  scene.add(fill);\n\n  controls = new THREE.OrbitControls(camera, canvas);\n  controls.enableDamping = true;\n  controls.dampingFactor = 0.08;\n  controls.autoRotate = false;\n\n  buildHelix();\n  setupRaycaster(canvas);\n\n  function animate() {\n    requestAnimationFrame(animate);\n    if (spinning && helixGroup) helixGroup.rotation.y += spinSpeed;\n    controls.update();\n    renderer.render(scene, camera);\n  }\n  animate();\n}\nif (window.THREE && THREE.OrbitControls) init();\n\nvar clickTargets = [];\nvar basePairInfo = {\n  'A': { name: '腺嘌呤 (Adenine)', pair: '与胸腺嘧啶 T 配对，形成 2 个氢键', color: '#7F77DD' },\n  'T': { name: '胸腺嘧啶 (Thymine)', pair: '与腺嘌呤 A 配对，形成 2 个氢键', color: '#AFA9EC' },\n  'G': { name: '鸟嘌呤 (Guanine)', pair: '与胞嘧啶 C 配对，形成 3 个氢键', color: '#1D9E75' },\n  'C': { name: '胞嘧啶 (Cytosine)', pair: '与鸟嘌呤 G 配对，形成 3 个氢键', color: '#5DCAA5' }\n};\n\nfunction buildHelix() {\n  helixGroup = new THREE.Group();\n  scene.add(helixGroup);\n\n  var strandMat1 = new THREE.MeshLambertMaterial({ color: 0x7F77DD });\n  var strandMat2 = new THREE.MeshLambertMaterial({ color: 0x534AB7 });\n  var phosphateMat1 = new THREE.MeshLambertMaterial({ color: 0xAFA9EC });\n  var phosphateMat2 = new THREE.MeshLambertMaterial({ color: 0x3C3489 });\n\n  var baseMats = {\n    'A': new THREE.MeshLambertMaterial({ color: 0x7F77DD }),\n    'T': new THREE.MeshLambertMaterial({ color: 0xAFA9EC }),\n    'G': new THREE.MeshLambertMaterial({ color: 0x1D9E75 }),\n    'C': new THREE.MeshLambertMaterial({ color: 0x5DCAA5 })\n  };\n  var bondMat = new THREE.MeshLambertMaterial({ color: 0x888780, transparent: true, opacity: 0.6 });\n\n  var numPairs = 20;\n  var rise = 0.68;\n  var radius = 2.2;\n  var twistPerStep = (2 * Math.PI) / 10;\n  var totalHeight = numPairs * rise;\n  var yOffset = -totalHeight / 2;\n\n  var sequence = ['A','T','G','C','A','G','T','C','G','A','T','G','C','T','A','C','G','A','T','C'];\n  var complement = { 'A':'T', 'T':'A', 'G':'C', 'C':'G' };\n\n  var strand1Points = [];\n  var strand2Points = [];\n\n  for (var i = 0; i < numPairs; i++) {\n    var angle = i * twistPerStep;\n    var y = yOffset + i * rise;\n    var x1 = Math.cos(angle) * radius;\n    var z1 = Math.sin(angle) * radius;\n    var x2 = Math.cos(angle + Math.PI) * radius;\n    var z2 = Math.sin(angle + Math.PI) * radius;\n    strand1Points.push(new THREE.Vector3(x1, y, z1));\n    strand2Points.push(new THREE.Vector3(x2, y, z2));\n\n    var base = sequence[i % sequence.length];\n    var comp = complement[base];\n\n    var sphereGeo = new THREE.SphereGeometry(0.28, 16, 12);\n    var s1 = new THREE.Mesh(sphereGeo, baseMats[base]);\n    s1.position.set(x1, y, z1);\n    s1.name = base;\n    helixGroup.add(s1);\n    clickTargets.push(s1);\n\n    var s2 = new THREE.Mesh(sphereGeo, baseMats[comp]);\n    s2.position.set(x2, y, z2);\n    s2.name = comp;\n    helixGroup.add(s2);\n    clickTargets.push(s2);\n\n    var p1 = new THREE.Vector3(x1, y, z1);\n    var p2 = new THREE.Vector3(x2, y, z2);\n    var bondLen = p1.distanceTo(p2);\n    var bondGeo = new THREE.CylinderGeometry(0.07, 0.07, bondLen, 8);\n    var bond = new THREE.Mesh(bondGeo, bondMat);\n    var mid = new THREE.Vector3().addVectors(p1, p2).multiplyScalar(0.5);\n    bond.position.copy(mid);\n    bond.lookAt(p1);\n    bond.rotateX(Math.PI / 2);\n    helixGroup.add(bond);\n\n    if (i > 0) {\n      addBackbone(strand1Points[i-1], strand1Points[i], strandMat1, phosphateMat1);\n      addBackbone(strand2Points[i-1], strand2Points[i], strandMat2, phosphateMat2);\n    }\n  }\n}\n\nfunction addBackbone(p1, p2, tubeMat, sphereMat) {\n  var len = p1.distanceTo(p2);\n  var geo = new THREE.CylinderGeometry(0.12, 0.12, len, 8);\n  var mesh = new THREE.Mesh(geo, tubeMat);\n  var mid = new THREE.Vector3().addVectors(p1, p2).multiplyScalar(0.5);\n  mesh.position.copy(mid);\n  mesh.lookAt(p2);\n  mesh.rotateX(Math.PI / 2);\n  helixGroup.add(mesh);\n\n  var sGeo = new THREE.SphereGeometry(0.18, 12, 8);\n  var s = new THREE.Mesh(sGeo, sphereMat);\n  s.position.copy(p1);\n  helixGroup.add(s);\n}\n\nfunction toggleSpin() {\n  spinning = !spinning;\n  document.querySelector('#controls button').textContent = spinning ? '暂停旋转' : '继续旋转';\n}\n\nfunction setupRaycaster(canvas) {\n  var raycaster = new THREE.Raycaster();\n  var mouse = new THREE.Vector2();\n  var dragDist = 0, startX, startY;\n  canvas.addEventListener('mousedown', function(e) { startX = e.clientX; startY = e.clientY; dragDist = 0; });\n  canvas.addEventListener('mousemove', function(e) { dragDist += Math.abs(e.clientX-(startX||0)) + Math.abs(e.clientY-(startY||0)); });\n  canvas.addEventListener('click', function(e) {\n    if (dragDist > 5) return;\n    var rect = canvas.getBoundingClientRect();\n    mouse.x = ((e.clientX - rect.left) / rect.width) * 2 - 1;\n    mouse.y = -((e.clientY - rect.top) / rect.height) * 2 + 1;\n    raycaster.setFromCamera(mouse, camera);\n    var hits = raycaster.intersectObjects(clickTargets);\n    if (hits.length) {\n      var name = hits[0].object.name;\n      window.__widgetSendMessage('详细介绍 DNA 碱基 ' + name + ' 的结构与功能');\n    }\n  });\n}\n</script>"}
```

点击任意碱基球体可深入了解该碱基的结构与配对规则。

颜色编码：紫色 = 腺嘌呤 A / 胸腺嘧啶 T（2 个氢键），青绿色 = 鸟嘌呤 G / 胞嘧啶 C（3 个氢键），深紫色骨架 = 磷酸糖链。每 10 个碱基对完成一圈螺旋，这是 B 型 DNA 的标准构型。