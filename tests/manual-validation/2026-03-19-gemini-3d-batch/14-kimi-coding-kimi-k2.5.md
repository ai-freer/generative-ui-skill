这是一个交互式3D太阳系模型。你可以拖拽旋转视角，点击任意行星了解详情，或使用下方的控制按钮调整视角和动画速度。

```show-widget
{"title": "solar_system_3d", "widget_code": "<style>\ncanvas { display: block; width: 100% !important; height: 420px !important; }\n#controls { display: flex; align-items: center; gap: 12px; padding: 10px 0 0; font-size: 13px; color: var(--color-text-secondary); flex-wrap: wrap; }\nbutton { background: var(--color-background-secondary); border: none; border-radius: 999px; padding: 6px 14px; font-size: 13px; cursor: pointer; color: var(--color-text-primary); }\nbutton:hover { background: var(--color-background-tertiary); }\n</style>\n<canvas id=\"c\"></canvas>\n<div id=\"controls\">\n  <button id=\"playBtn\" onclick=\"togglePlay()\">暂停</button>\n  <span id=\"speedLabel\" style=\"flex:1\">速度: 1x</span>\n  <label style=\"display:flex;align-items:center;gap:8px\">\n    <input type=\"range\" min=\"1\" max=\"5\" value=\"2\" step=\"1\" style=\"width:80px\" oninput=\"setSpeed(this.value)\">\n  </label>\n  <button onclick=\"setView('top')\">俯视</button>\n  <button onclick=\"setView('side')\">侧面</button>\n  <button onclick=\"setView('front')\">正面</button>\n</div>\n<script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js\" onload=\"init()\"></script>\n<script>\nvar scene, camera, renderer, controls;\nvar planets = [], orbits = [];\nvar playing = true, speedMul = 1;\nvar raycaster = new THREE.Raycaster();\nvar mouse = new THREE.Vector2();\nvar dragDist = 0, startX, startY;\n\nfunction init() {\n  if (!window.THREE || !THREE.OrbitControls) return;\n  var canvas = document.getElementById('c');\n  var W = canvas.clientWidth, H = 420;\n  renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true, alpha: true });\n  renderer.setSize(W, H);\n  renderer.setPixelRatio(Math.min(devicePixelRatio, 2));\n\n  scene = new THREE.Scene();\n  camera = new THREE.PerspectiveCamera(45, W / H, 0.1, 1000);\n  camera.position.set(0, 12, 28);\n\n  scene.add(new THREE.AmbientLight(0xffffff, 0.6));\n  var sun = new THREE.DirectionalLight(0xffffff, 0.9);\n  sun.position.set(5, 8, 5);\n  scene.add(sun);\n\n  controls = new THREE.OrbitControls(camera, canvas);\n  controls.enableDamping = true;\n  controls.dampingFactor = 0.08;\n\n  canvas.addEventListener('mousedown', function(e) { startX = e.clientX; startY = e.clientY; dragDist = 0; });\n  canvas.addEventListener('mousemove', function(e) { dragDist += Math.abs(e.clientX - startX) + Math.abs(e.clientY - startY); });\n  canvas.addEventListener('click', onCanvasClick);\n\n  animate();\n}\n\nif (window.THREE && THREE.OrbitControls) init();\n\n// 太阳\nvar sunGeo = new THREE.SphereGeometry(1.8, 32, 32);\nvar sunMat = new THREE.MeshBasicMaterial({ color: 0xEF9F27 });\nvar sunMesh = new THREE.Mesh(sunGeo, sunMat);\nsunMesh.name = \"太阳\";\nscene.add(sunMesh);\n\n// 行星数据: 名称, 颜色, 半径, 轨道半径, 公转速度系数\nvar planetData = [\n  { name: \"水星\", color: 0x888780, r: 0.4, dist: 4, speed: 4.15 },\n  { name: \"金星\", color: 0x1D9E75, r: 0.6, dist: 6, speed: 1.62 },\n  { name: \"地球\", color: 0x378ADD, r: 0.65, dist: 8, speed: 1.0 },\n  { name: \"火星\", color: 0xD85A30, r: 0.5, dist: 10.5, speed: 0.53 },\n  { name: \"木星\", color: 0x7F77DD, r: 1.2, dist: 14, speed: 0.084 },\n  { name: \"土星\", color: 0xEF9F27, r: 1.0, dist: 18, speed: 0.034, hasRing: true }\n];\n\nplanetData.forEach(function(d) {\n  // 创建轨道环\n  var orbitGeo = new THREE.RingGeometry(d.dist - 0.02, d.dist + 0.02, 64);\n  var orbitMat = new THREE.MeshBasicMaterial({ color: 0x888780, side: THREE.DoubleSide, transparent: true, opacity: 0.15 });\n  var orbitMesh = new THREE.Mesh(orbitGeo, orbitMat);\n  orbitMesh.rotation.x = Math.PI / 2;\n  scene.add(orbitMesh);\n  \n  // 创建行星组（用于公转）\n  var group = new THREE.Group();\n  scene.add(group);\n  \n  // 创建行星本体\n  var geo = new THREE.SphereGeometry(d.r, 24, 24);\n  var mat = new THREE.MeshLambertMaterial({ color: d.color });\n  var mesh = new THREE.Mesh(geo, mat);\n  mesh.position.x = d.dist;\n  mesh.name = d.name;\n  group.add(mesh);\n  \n  // 土星环\n  if (d.hasRing) {\n    var ringGeo = new THREE.RingGeometry(d.r + 0.3, d.r + 0.8, 32);\n    var ringMat = new THREE.MeshBasicMaterial({ color: 0xD3D1C7, side: THREE.DoubleSide, transparent: true, opacity: 0.6 });\n    var ring = new THREE.Mesh(ringGeo, ringMat);\n    ring.rotation.x = Math.PI / 2;\n    mesh.add(ring);\n  }\n  \n  planets.push({ group: group, mesh: mesh, speed: d.speed, angle: Math.random() * Math.PI * 2 });\n});\n\nfunction animate() {\n  requestAnimationFrame(animate);\n  \n  if (playing) {\n    planets.forEach(function(p) {\n      p.angle += 0.005 * p.speed * speedMul;\n      p.group.rotation.y = p.angle;\n    });\n  }\n  \n  controls.update();\n  renderer.render(scene, camera);\n}\n\nfunction togglePlay() {\n  playing = !playing;\n  document.getElementById('playBtn').textContent = playing ? '暂停' : '播放';\n}\n\nfunction setSpeed(v) {\n  speedMul = parseInt(v);\n  document.getElementById('speedLabel').textContent = '速度: ' + speedMul + 'x';\n}\n\nfunction setView(v) {\n  var pos = { top: [0, 28, 0.1], side: [28, 2, 0], front: [0, 2, 28] }[v];\n  camera.position.set(pos[0], pos[1], pos[2]);\n  camera.lookAt(0, 0, 0);\n  controls.update();\n}\n\nfunction onCanvasClick(e) {\n  if (dragDist > 5) return;\n  var canvas = document.getElementById('c');\n  var rect = canvas.getBoundingClientRect();\n  mouse.x = ((e.clientX - rect.left) / rect.width) * 2 - 1;\n  mouse.y = -((e.clientY - rect.top) / rect.height) * 2 + 1;\n  \n  raycaster.setFromCamera(mouse, camera);\n  var targets = [sunMesh].concat(planets.map(function(p) { return p.mesh; }));\n  var hits = raycaster.intersectObjects(targets);\n  \n  if (hits.length > 0) {\n    var name = hits[0].object.name;\n    window.__widgetSendMessage('详细介绍' + name + '的特点和运行轨道');\n  }\n}\n</script>"}
```

**操作提示：**
- **旋转视角**：按住鼠标左键拖拽
- **缩放**：使用鼠标滚轮
- **查看详情**：点击任意行星或太阳
- **视角切换**：使用下方按钮快速切换到俯视、侧面或正面视角
- **速度调节**：拖动滑块加快或减慢行星公转速度

模型展示了类地行星（水星、金星、地球、火星）与气态巨行星（木星、土星）的相对大小和轨道距离比例。土星带有光环结构，太阳使用自发光材质模拟恒星亮度。