DNA 双螺旋可以理解为“两条缠绕的骨架 + 中间一层层碱基对台阶”。下面这个 3D 模型支持拖拽旋转、自动转动、调节转速和切换视角；点击骨架或中间的“台阶”还能继续深入看结构说明。

```show-widget
{"title":"dna_double_helix_3d","widget_code":"<style>\ncanvas { display:block; width:100% !important; height:420px !important; }\n#controls { display:flex; align-items:center; gap:12px; padding:10px 0 0; font-size:13px; color:var(--color-text-secondary); flex-wrap:wrap; }\n#legend { display:flex; gap:14px; flex-wrap:wrap; padding-top:8px; font-size:12px; color:var(--color-text-secondary); }\n#legend span { display:flex; align-items:center; gap:6px; }\n#legend i { width:10px; height:10px; border-radius:2px; display:inline-block; }\n</style>\n<canvas id=\"c\"></canvas>\n<div id=\"controls\">\n  <button id=\"btn\" onclick=\"toggleSpin()\">Pause</button>\n  <button onclick=\"setView('front')\">Front</button>\n  <button onclick=\"setView('side')\">Side</button>\n  <button onclick=\"setView('top')\">Top</button>\n  <label style=\"display:flex;align-items:center;gap:8px;flex:1;min-width:220px;\">\n    Speed\n    <input type=\"range\" min=\"1\" max=\"10\" value=\"5\" step=\"1\" style=\"flex:1\" oninput=\"spinMul=Number(this.value)/10; document.getElementById('speedOut').textContent=Math.round(spinMul*10)/10 + '×';\">\n  </label>\n  <span id=\"speedOut\">0.5×</span>\n</div>\n<div id=\"legend\">\n  <span><i style=\"background:#7F77DD\"></i>Backbone A</span>\n  <span><i style=\"background:#1D9E75\"></i>Backbone B</span>\n  <span><i style=\"background:#D85A30\"></i>A–T pair</span>\n  <span><i style=\"background:#EF9F27\"></i>G–C pair</span>\n</div>\n<script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js\" onload=\"init()\"></script>\n<script>\nvar scene, camera, renderer, controls, helixGroup;\nvar spinMul = 0.5, spinning = true;\nvar clickTargets = [];\nvar raycaster = new THREE.Raycaster();\nvar mouse = new THREE.Vector2();\nvar dragDist = 0, lastX = 0, lastY = 0;\nfunction init() {\n  if (!window.THREE || !THREE.OrbitControls || renderer) return;\n  var canvas = document.getElementById('c');\n  var W = canvas.clientWidth || 680, H = 420;\n  renderer = new THREE.WebGLRenderer({ canvas: canvas, antialias: true, alpha: true });\n  renderer.setSize(W, H);\n  renderer.setPixelRatio(Math.min(devicePixelRatio, 2));\n  scene = new THREE.Scene();\n  camera = new THREE.PerspectiveCamera(45, W / H, 0.1, 1000);\n  camera.position.set(0, 4, 13);\n  scene.add(new THREE.AmbientLight(0xffffff, 0.7));\n  var sun = new THREE.DirectionalLight(0xffffff, 0.9);\n  sun.position.set(5, 8, 6);\n  scene.add(sun);\n  controls = new THREE.OrbitControls(camera, canvas);\n  controls.enableDamping = true;\n  controls.dampingFactor = 0.08;\n  controls.autoRotate = true;\n  controls.autoRotateSpeed = 0.6;\n  canvas.addEventListener('mousedown', function(e){ dragDist = 0; lastX = e.clientX; lastY = e.clientY; });\n  canvas.addEventListener('mousemove', function(e){ dragDist += Math.abs(e.clientX - lastX) + Math.abs(e.clientY - lastY); lastX = e.clientX; lastY = e.clientY; });\n  canvas.addEventListener('click', onCanvasClick);\n  window.addEventListener('resize', resizeScene);\n  buildDNA();\n  animate();\n}\nfunction resizeScene() {\n  if (!renderer || !camera) return;\n  var canvas = document.getElementById('c');\n  var W = canvas.clientWidth || 680, H = 420;\n  renderer.setSize(W, H);\n  camera.aspect = W / H;\n  camera.updateProjectionMatrix();\n}\nfunction toggleSpin() {\n  spinning = !spinning;\n  controls.autoRotate = spinning;\n  document.getElementById('btn').textContent = spinning ? 'Pause' : 'Play';\n}\nfunction setView(v) {\n  var pos = { front:[0,4,13], side:[13,3,0], top:[0,15,0.1] }[v];\n  camera.position.set(pos[0], pos[1], pos[2]);\n  camera.lookAt(0, 0, 0);\n  controls.update();\n}\nfunction makeCylinder(a, b, radius, color, name, prompt) {\n  var dir = new THREE.Vector3().subVectors(b, a);\n  var len = dir.length();\n  var geom = new THREE.CylinderGeometry(radius, radius, len, 12);\n  var mat = new THREE.MeshLambertMaterial({ color: color });\n  var mesh = new THREE.Mesh(geom, mat);\n  var mid = new THREE.Vector3().addVectors(a, b).multiplyScalar(0.5);\n  mesh.position.copy(mid);\n  mesh.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0), dir.clone().normalize());\n  mesh.name = name;\n  mesh.userData.prompt = prompt;\n  clickTargets.push(mesh);\n  return mesh;\n}\nfunction buildDNA() {\n  helixGroup = new THREE.Group();\n  scene.add(helixGroup);\n  clickTargets = [];\n  var backboneA = 0x7F77DD;\n  var backboneB = 0x1D9E75;\n  var pairAT = 0xD85A30;\n  var pairGC = 0xEF9F27;\n  var gray = 0x888780;\n  var radius = 2.2;\n  var turns = 3;\n  var pairs = 12;\n  var height = 8.4;\n  var stepY = height / (pairs - 1);\n  var stepAngle = Math.PI * 2 * turns / (pairs - 1);\n  var sphereGeom = new THREE.SphereGeometry(0.24, 16, 12);\n  for (var i = 0; i < pairs; i++) {\n    var t = i * stepAngle;\n    var y = -height / 2 + i * stepY;\n    var p1 = new THREE.Vector3(Math.cos(t) * radius, y, Math.sin(t) * radius);\n    var p2 = new THREE.Vector3(Math.cos(t + Math.PI) * radius, y, Math.sin(t + Math.PI) * radius);\n    var m1 = new THREE.MeshLambertMaterial({ color: backboneA });\n    var m2 = new THREE.MeshLambertMaterial({ color: backboneB });\n    var s1 = new THREE.Mesh(sphereGeom, m1);\n    var s2 = new THREE.Mesh(sphereGeom, m2);\n    s1.position.copy(p1);\n    s2.position.copy(p2);\n    s1.name = 'Backbone A';\n    s2.name = 'Backbone B';\n    s1.userData.prompt = '详细介绍 DNA 的糖磷酸骨架';\n    s2.userData.prompt = '详细介绍 DNA 的糖磷酸骨架';\n    clickTargets.push(s1, s2);\n    helixGroup.add(s1, s2);\n    if (i < pairs - 1) {\n      var t2 = (i + 1) * stepAngle;\n      var y2 = -height / 2 + (i + 1) * stepY;\n      var n1 = new THREE.Vector3(Math.cos(t2) * radius, y2, Math.sin(t2) * radius);\n      var n2 = new THREE.Vector3(Math.cos(t2 + Math.PI) * radius, y2, Math.sin(t2 + Math.PI) * radius);\n      helixGroup.add(makeCylinder(p1, n1, 0.09, backboneA, 'Backbone A segment', '详细介绍 DNA 的糖磷酸骨架'));\n      helixGroup.add(makeCylinder(p2, n2, 0.09, backboneB, 'Backbone B segment', '详细介绍 DNA 的糖磷酸骨架'));\n    }\n    var pairColor = i % 2 === 0 ? pairAT : pairGC;\n    var pairName = i % 2 === 0 ? 'A–T base pair' : 'G–C base pair';\n    var pairPrompt = i % 2 === 0 ? '详细介绍 DNA 中 A 和 T 如何配对' : '详细介绍 DNA 中 G 和 C 如何配对';\n    var rung = makeCylinder(p1, p2, 0.11, pairColor, pairName, pairPrompt);\n    helixGroup.add(rung);\n  }\n  var axisGeom = new THREE.CylinderGeometry(0.03, 0.03, 9.2, 10);\n  var axisMat = new THREE.MeshBasicMaterial({ color: gray, transparent: true, opacity: 0.35 });\n  var axis = new THREE.Mesh(axisGeom, axisMat);\n  axis.name = 'Helix axis';\n  axis.userData.prompt = '详细介绍 DNA 双螺旋的空间结构';\n  clickTargets.push(axis);\n  helixGroup.add(axis);\n  var ringGeom = new THREE.TorusGeometry(2.2, 0.01, 8, 48);\n  for (var j = 0; j < 3; j++) {\n    var ring = new THREE.Mesh(ringGeom, new THREE.MeshBasicMaterial({ color: gray, transparent: true, opacity: 0.18 }));\n    ring.rotation.x = Math.PI / 2;\n    ring.position.y = -2.8 + j * 2.8;\n    helixGroup.add(ring);\n  }\n}\nfunction onCanvasClick(e) {\n  if (dragDist > 5 || !renderer || !camera) return;\n  var canvas = document.getElementById('c');\n  var rect = canvas.getBoundingClientRect();\n  mouse.x = ((e.clientX - rect.left) / rect.width) * 2 - 1;\n  mouse.y = -((e.clientY - rect.top) / rect.height) * 2 + 1;\n  raycaster.setFromCamera(mouse, camera);\n  var hits = raycaster.intersectObjects(clickTargets);\n  if (hits.length) {\n    var prompt = hits[0].object.userData.prompt || ('详细介绍 ' + hits[0].object.name);\n    window.__widgetSendMessage(prompt);\n  }\n}\nfunction animate() {\n  requestAnimationFrame(animate);\n  if (helixGroup) helixGroup.rotation.y += 0.003 * spinMul;\n  controls.autoRotateSpeed = 0.6 * spinMul * 2;\n  controls.update();\n  renderer.render(scene, camera);\n}\nif (window.THREE && THREE.OrbitControls) init();\n</script>"}
```

这个模型重点展示的是“双螺旋的几何关系”，不是原子级精细化学建模，所以更适合建立直觉。你可以点击中间的碱基对或两侧骨架，我可以继续展开讲 `A-T / G-C 配对`、`糖磷酸骨架`、`为什么是双螺旋`。